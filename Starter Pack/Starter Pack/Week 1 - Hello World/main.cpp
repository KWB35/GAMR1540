#include <iostream> //Using someone elses code via iostream.h

using namespace std;//using std nameplate gives ease of access to things in iostream

int main() //initialises the function main() 
{
    cout << "Hello World!\n" << endl; //prints "Hello World!" to the console window and makes it so that anything printed afterwards in on a seperate line
    
    system("PAUSE"); //pauses the program
    return 0; //Tells the OS the program ran succesfully and allows for the program to detect errors as if you return anything other than zero an error has occured
}

